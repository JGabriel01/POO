/* Outra maneira para exportar os módulos no Ecma script */
export function maisculo(nome) {
    return nome.toUpperCase() /* Retoma o nome maisculo */
}
/* Para se usar export e import, o arquivo com export tem que ter .mjs */